# agents.md

## --project-truth の階層の情報

- この階層は project truth に準ずる文書を保持する。


## --project-truth に含まれるファイルとその内容

- 直下に `prj-<project名>/` として、プロジェクト毎のディレクトリを持つ
- `prj-<project名>/` 直下には `agents.md` を置かない
