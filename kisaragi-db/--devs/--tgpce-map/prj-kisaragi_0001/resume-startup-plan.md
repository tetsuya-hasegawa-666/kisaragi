# resume-startup-plan

## 目的

- `prj-kisaragi_0001` を中断後に再開する時、現在地と立ち上げ順を短く確認できるようにする。

## 再開手順

1. `project-truth.md` で viewer contract を確認する
2. `ux-b2t-hypo.md` の `current_state` を確認する
3. `admin-mrl-test-method.md` に従い `npm test` と `npm run dashboard` を再確認する

## 現在地

- baseline は `MRL-1` から `MRL-4` まで `pass`
- main focus は launcher / dual-pane / read-only boundary の維持である
