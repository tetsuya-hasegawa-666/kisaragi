# resume-startup-plan

## 目的

- `prj-kisaragi_0003` を中断後に再開する時、reachability、recovery contract、evidence 導線の現在地を短く確認できるようにする。

## 再開手順

1. `project-truth.md` で主系統 / 副系統 / evidence 契約を確認する
2. `ux-b2t-hypo.md` の `current_state` を確認する
3. `admin-mrl-test-method.md` に従って `doctor`、`resume`、`/codex status` の導線を再確認する

## 現在地

- `MRL-1` から `MRL-7` は `pass`
- current focus は運用の維持と add vision の取捨選択である
