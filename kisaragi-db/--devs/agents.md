# agents.md

この directory は、`codev-db` 配下の category directory として扱うものとする。

## 基本

- 最上位方針は `codev-db/AGENTS.md` を参照するものとする。
- この directory 配下は category 単位の共通 rule と pointer を置くものとする。
- project の実データは category ごとに project 配下へ置くものとする。
