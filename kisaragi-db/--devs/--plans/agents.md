# agents.md

## --plans の役割

- この階層は、project ごとの BDD、TDD、MRL / mRL 計画文書を保持する。

## 運用規則

- `prj-remote-pwsh` の `bdd-release-compass.md`、`tdd-test-matrix.md`、`mrl-record.md` を参照型とする。
- release 計画は `prj-<project名>/` ごとにまとめる。
